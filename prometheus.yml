# Scrape config for running the Prometheus binary directly on your machine
# (`prometheus --config.file=prometheus.yml`).
#
# The containerised stack started by `make monitoring` does not use this file:
# it uses monitoring/prometheus.yml, which differs only in reaching spectroperf
# on the host via host.docker.internal rather than localhost.

global:
  scrape_interval:     15s # By default, scrape targets every 15 seconds.

  # Attach these labels to any time series or alerts when communicating with
  # external systems (federation, remote storage, Alertmanager).
  external_labels:
    monitor: 'codelab-monitor'

# A scrape configuration containing exactly one endpoint to scrape:
# Here it's Prometheus itself.
scrape_configs:
  # The job name is added as a label `job=<job_name>` to any timeseries scraped from this config.
  - job_name: 'prometheus'

    # Override the global default and scrape targets from this job every 5 seconds.
    scrape_interval: 5s

    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'spectroperf'
    # Override the global default and scrape targets from this job every 5 seconds.
    scrape_interval: 5s

    static_configs:
      - targets: ['localhost:2112']
        labels:
          group: 'spectroperf'